

1. Capitation
 a. CapitationGroup   
    - name
    - description
    - lower_date_of_enrollment
    
 b. Capitation
    - capitation_group_id
    - funding_type_id
    - toal_enrollees
    - total_amount
    - capitation_rate
    -created_at
    -updated_at
    - review_status
    - reviewed_at
    - reviewd_by
    - approval_status
    - approved_at
    - approved_by
    - paid_status
    - paid_at
    - paid_by

2. PreAuthorization
  a.PreAuthorization
   - name
   - code
   - created_at
   - updated_at
   - code_type (1=referal, 2=authorization code)
   - status (1=approved, 0=pending, 2=expired/cancelled, 3=rejected)
   - expiry_date
   - review_comment (nullable)
   - requester_comment (nullable)

b. PreAuthorizationServices
  - preauthorization_id
  - service_id
  - created_at
  - updated_at

3. Services
  a. Service
   - name
   - code
   - created_at
   - updated_at
   - status (1=active, 2=inactive)
   - description
   - service_category (1=primary, 2=secondary)
   - service_type (1=enrollment, 2=payment, 3=other)
   - payment_type (1=tariff, 2=other)
